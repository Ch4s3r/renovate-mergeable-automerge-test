version: 2
mergeable:
  - when: pull_request.*
    name: 'Single commit with mergeable@test email'
    validate:
      - do: commit
        message:
          regex: '^patrick.lampe.dittmer@gmail.com$'
          message_type: committer_email
